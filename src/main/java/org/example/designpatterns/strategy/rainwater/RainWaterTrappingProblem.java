package org.example.designpatterns.strategy.rainwater;

import java.util.List;

public class RainWaterTrappingProblem {
    int solve(List<Integer> heights, String tc, String sc) {
        if (tc.equals("O(n^2)")) {
            // brute force
        } else if (tc.equals("O(n)") && sc.equals("O(n)")) {
            // optimal
        } else {
            // most optimal
        }
        return 0;
    }
}
