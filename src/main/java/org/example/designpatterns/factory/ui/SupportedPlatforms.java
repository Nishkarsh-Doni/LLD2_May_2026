package org.example.designpatterns.factory.ui;

/**
 * SupportedPlatforms — enum of platforms the UI framework supports.
 * Used by the Simple Factory to decide which UIFactory to create.
 */
public enum SupportedPlatforms {
    ANDROID,
    IOS
}
